replicaCount: 2

image:
  repository: ghcr.io/carbonell-castillo/authorization-service
  tag: "1.0.0"

service:
  port: 4001
  targetPort: 4001

health:
  path: /api/health

env:
  LOG_LEVEL: info
  PORT: "4001"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
